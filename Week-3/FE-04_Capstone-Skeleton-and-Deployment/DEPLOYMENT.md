# Deployment

For Vercel: push the repository to GitHub, import it into Vercel, and use `app` as the root directory when the FE-04 parent folder is the repository root. Build command: `npm run build`. Verify all routes after deployment.
