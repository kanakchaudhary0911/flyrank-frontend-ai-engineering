import {useRef,useState} from "react";
export interface TabItem{id:string;label:string;content:React.ReactNode} interface TabsProps{tabs:TabItem[];label?:string}
export function Tabs({tabs,label="Example tabs"}:TabsProps){const[active,setActive]=useState(0),refs=useRef<Array<HTMLButtonElement|null>>([]);const move=(i:number)=>{const n=(i+tabs.length)%tabs.length;setActive(n);refs.current[n]?.focus()};const key=(e:React.KeyboardEvent<HTMLButtonElement>,i:number)=>{if(e.key==="ArrowRight"||e.key==="ArrowDown"){e.preventDefault();move(i+1)}else if(e.key==="ArrowLeft"||e.key==="ArrowUp"){e.preventDefault();move(i-1)}else if(e.key==="Home"){e.preventDefault();move(0)}else if(e.key==="End"){e.preventDefault();move(tabs.length-1)}};return <section aria-label={label}><div role="tablist" aria-label={label}>{tabs.map((t,i)=><button key={t.id} ref={e=>{refs.current[i]=e}} id={`tab-${t.id}`} role="tab" aria-selected={active===i} aria-controls={`panel-${t.id}`} tabIndex={active===i?0:-1} onClick={()=>setActive(i)} onKeyDown={e=>key(e,i)}>{t.label}</button>)}</div>{tabs.map((t,i)=><div key={t.id} id={`panel-${t.id}`} role="tabpanel" aria-labelledby={`tab-${t.id}`} hidden={active!==i} tabIndex={0}>{t.content}</div>)}</section>}